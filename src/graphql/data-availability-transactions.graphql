query DataAvailabilityTransactions($owners: [String!], $after: String) {
  transactions(owners: $owners, limit: 1000, after: $after, order: ASC) {
    edges {
      node {
        id
        address
      }
      cursor
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
