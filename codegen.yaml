schema: https://node1.bundlr.network/graphql
documents: './src/graphql/*.graphql'
generates:
  ./verifier/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      fetcher: fetch
